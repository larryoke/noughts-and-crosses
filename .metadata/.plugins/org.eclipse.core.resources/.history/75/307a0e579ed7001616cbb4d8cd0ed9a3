package gameClient.view;

public enum Square {
    X, O, EMPTY;

    @Override
    public String toString() {
	if (X.equals(this)) {
	    return "X";
	} else if (O.equals(this)) {
	    return "O";

	} else
	    return " ";
    }
}
